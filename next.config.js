/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "images.pexels.com",
      "www.pexels.com",
      "https://ambroseportfolio.netlify.app/",
    ],
  },
};

module.exports = nextConfig;
